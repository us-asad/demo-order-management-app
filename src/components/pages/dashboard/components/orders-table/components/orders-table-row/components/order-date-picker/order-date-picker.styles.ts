import { makeStyles } from "@mui/styles";

export const useOrderDatePicker = makeStyles({
  formController: { margin: 8, display: "flex", justifyContent: "start" },
  datePickerField: { transform: "scale(.8)" }
});