import { makeStyles } from "@mui/styles";

export const useOrdersHeaderFilter = makeStyles({
  wrapper: { display: "flex", gap: 16 },
  dateRangePicker: { width: 300 },
  selectController: { width: 170 },
  searchField: { width: 200 }
});