import { makeStyles } from "@mui/styles";

export const useHeaderStyles = makeStyles({
  wrapper: { flexGrow: 1 },
  title: { flexGrow: 1 },
  buttonsWrapper: { display: "flex", alignItems: "center", gap: 24 }
});